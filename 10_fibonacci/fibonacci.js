const fibonacci = function(num) {
    const arr = [1]
    for (let i = 1; i < 5; i++) {
        let series = arr.reduce((total, currentvalue) => , 0)
        arr.push(series)
        console.log(arr)
    }
    
};

console.log(fibonacci(2))

// Do not edit below this line
module.exports = fibonacci;
